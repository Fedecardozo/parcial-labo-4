export class Pais {
  nombre: string;
  region: string;
  bandera: string;

  constructor(nombre: string, region: string, bandera: string) {
    this.nombre = nombre;
    this.region = region;
    this.bandera = bandera;
  }
}
